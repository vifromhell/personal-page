# Codex instructions

- This repository is the personal portfolio site of a Design System Designer.
- Read `PROJECT_CONTEXT.md` before making any changes.
- Preserve the established visual concept unless the user explicitly asks to change it.
- Do not create commits or push changes unless the user gives a separate, explicit command to do so.
- Preserve the responsive approach and verify changes against the 1440 px desktop and 390 px mobile reference widths.
- Do not add dependencies unless they are necessary for the requested work.
- Keep project code separate from project documentation and content-source mapping.
- Treat Notion as the intended content source, but never expose Notion credentials in client-side code or committed files.
